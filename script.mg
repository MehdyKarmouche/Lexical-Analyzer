# Function definition
func add(a:int, b:car) int {
        car c := 's'.
        int a := 1, b, c.
        return a.
}

# Main program
inizio{
    costante PI := 3.
    ind y.
    int result, a := 2.
    vettore(int:5) := [0, 1, 3].
    vettore(car:5) := "abcd@!234".
    
    result := add(a, PI).
    mentre: x < y {
        se: (a > b) e (3 <= 4) {
            print(a).
        }
        altro{
            print(b).
        }
    }
}